var config = {};

config.tmpDir = 'tmp';

// path to files uploaded by user
config.uploadPath = 'tmp/uploads';

// path to generated encoding files
config.outputPath = 'tmp/output/';

module.exports = config;
